const people = ['bob', 'mark', 'greg', 'tim']
const ages = [20, 25, 30, 35]

console.log(people);

module.exports = {
    people,
    ages //this is shorthand for "people: people", creates property called people and sets value to people
}